# PR47 summary

Pages incluidas:

- `Pag50032.borrar.al`
- `Pag50034.Procesarfacturas1.al`
- `Pag50035.ExpedientesLotes.al`

Cambios:

- Sustitución de referencias numéricas estándar identificadas por nombres (`Cartera Doc.`, `Integer`).
- Normalización de `SourceTableView`.
- Sustitución de campos de variables calculadas desde `Rec.<variable>` a variables locales reales.
- Normalización de casing AL en `Clear`, `SetRange`, `FindFirst`, `CalcFields` y `Format`.
- Conservación de lógica funcional original.
- No se modifican objetos del rango `90000-90999`.

Pendiente: compilar con AL-Go.
