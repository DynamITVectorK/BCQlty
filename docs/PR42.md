# PR42 summary

Pages incluidas:

- `Pag50074.ListaFacturaElectrnicaReg.al`

Cambios:

- Normalización de `SourceTableView`.
- Sustitución de `repeater()` anónimo por control con nombre.
- Sustitución de `Promoted`, `PromotedCategory` y `PromotedIsBig` por `area(Promoted)` con `actionref`.
- Eliminación de `[Scope('Internal')]`.
- Eliminación de declaraciones `Automation` y `File` incompatibles con BC SaaS en `fRechazarFacturaE`.
- El flujo SOAP legacy de rechazo queda bloqueado con error explícito SaaS hasta sustituirlo por `HttpClient`/`XmlDocument`.
- No se modifican objetos del rango `90000-90999`.

Pendiente: compilar con AL-Go.
