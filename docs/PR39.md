# PR39 summary

Pages incluidas:

- `Pag50905.ObservacionesPDF.al`

Cambios:

- Añadido `PageType = StandardDialog`.
- Sustituido origen de campo desde `Rec.vObservaciones` a variable local `vObservaciones`.
- Normalizado casing AL en llamadas de página, filtros y acciones.
- Eliminados atributos `[Scope('Internal')]`.
- No se modifican objetos del rango `90000–90999`.

Pendiente: compilar con AL-Go.
