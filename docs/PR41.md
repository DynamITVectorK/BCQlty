# PR41 summary

Pages incluidas:

- `Pag50064.LneasFacturaE.al`

Cambios:

- Normalización de casing AL.
- Sustitución de `repeater()` anónimo por control con nombre.
- Eliminación de atributos `[InDataSet]` y `[Scope('Internal')]`.
- Sustitución de Page 5601 por `Page "Fixed Asset List"`.
- Normalización de accesos a campos con `Rec.`.
- No se modifican objetos del rango `90000-90999`.

Pendiente: compilar con AL-Go.
