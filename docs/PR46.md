# PR46 summary

Pages incluidas:

- `Pag50022.WSBancoscliente.al`
- `Pag50024.WSHistoricodeabonos.al`
- `Pag50030.registrarFactura.al`

Cambios:

- Sustituidas referencias erróneas a variables de page como `Rec.<variable>` por variables locales reales.
- Normalización de `SourceTableView`.
- Normalización de casing AL en `Clear`, `Get`, `CalcDate`, `Format`, `WorkDate`, `SetFilter`, `SetRange`, `CalcFields`, `FindFirst`, `FindSet`, `Next` y `Commit`.
- Conservación de la lógica funcional original de WS de bancos, histórico de abonos y registro ADOS.
- No se modifican objetos del rango `90000-90999`.

Revisados sin tocar en esta tanda por no presentar cambios necesarios inmediatos:

- `Pag50020.LineasAbonosRegistrados.al`
- `Pag50029.DptoSolicitante.al`

Pendiente: compilar con AL-Go.
